{% layout 'theme' %}
{% section 'breadcrumb' %}

<section class="page-content">
  <div class="container">
    <h1 class="page-title">{{ page.title }}</h1>
    <div class="page-body">
      {{ page.content }}
    </div>
  </div>
</section>